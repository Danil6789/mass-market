spring:
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.marketplace.order: DEBUG
    org.hibernate.SQL: DEBUG

management:
  tracing:
    sampling:
      probability: 1.0
