server:
  port: 8084
  shutdown: graceful

spring:
  application:
    name: notification-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  # ---------- Database (PostgreSQL per-service) ----------
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5435}/${POSTGRES_DB:notification_db}}
    username: ${POSTGRES_USER:notification_svc}
    password: ${POSTGRES_PASSWORD:notification_pwd}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    placeholder-replacement: false

  # ---------- Kafka ----------
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: notification-group
      auto-offset-reset: latest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "com.marketplace.common.event"
        spring.json.use.type.headers: true
    listener:
      ack-mode: manual
      missing-topics-fatal: false

  # ---------- Mail (MailHog SMTP for dev) ----------
  mail:
    host: ${MAILHOG_HOST:localhost}
    port: ${MAILHOG_PORT:1025}
    username: ${MAILHOG_USER:}
    password: ${MAILHOG_PASSWORD:}
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

  # ---------- Thymeleaf (auto-configured; we only need string-mode resolver) ----------
  thymeleaf:
    cache: true
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html

# ---------- Eureka client ----------
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
    health-check:
      enabled: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

# ---------- JWT ----------
app:
  jwt:
    secret: ${JWT_SECRET:change-me-min-32-chars-long-secret-key-please}
    issuer: marketplace
    access-token-ttl-minutes: ${JWT_ACCESS_TTL_MINUTES:15}
    refresh-token-ttl-days: ${JWT_REFRESH_TTL_DAYS:7}

  mail:
    from: ${MAIL_FROM:noreply@marketplace.local}

  # ---------- Inter-service clients ----------
  clients:
    user-service: ${USER_SERVICE_URL:http://localhost:8081}

# ---------- SpringDoc ----------
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  packages-to-scan: com.marketplace.notification

# ---------- Actuator / Tracing ----------
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true
  tracing:
    sampling:
      probability: ${TRACING_SAMPLING_PROBABILITY:1.0}
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_ENDPOINT:http://localhost:9411/api/v2/spans}

logging:
  level:
    root: INFO
    com.marketplace: DEBUG
    org.springframework.kafka: WARN
